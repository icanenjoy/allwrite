import { Button } from "@mui/material";
import React from "react";

const WriteButton = () => {
  return (
    <Button variant="contained" sx={{ backgroundColor: "#e3b3ff" }}>
      작성하기
    </Button>
  );
};

export default WriteButton;
